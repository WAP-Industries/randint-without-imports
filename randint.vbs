Set Handler= CreateObject("Scripting.FileSystemObject"):Root = Replace(Handler.GetParentFolderName(WScript.ScriptFullName)+"\","\", "\\"):NPath = Root+"number.txt":Set RFile = Handler.CreateTextFile(Root+"randint.py", True):code = "def randint(l, u):" + vbCrLf + "    c = ''" + vbCrLf + "    while not bool(len(str(c))):" + vbCrLf + "        with open('" + NPath + "', 'r') as file:" + vbCrLf + "            c = file.read()" + vbCrLf + "    return int(u*float(c)+l)":RFile.Write code:RFile.Close:do while 1:Randomize:Set NFile = Handler.CreateTextFile(NPath, True):NFile.Write Rnd:NFile.Close:loop